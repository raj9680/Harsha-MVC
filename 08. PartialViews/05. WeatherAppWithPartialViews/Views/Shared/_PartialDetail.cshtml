@using WeatherApp.Models;

@model CityModel;

@if (Model != null)
{
    <h3>@Model.CityUniqueCode</h3>
    <p>@Model.CityName</p>
    <p>Temperature - @Model.TemperatureFahrenheit Fahrenheit</p>
    <small>Date - @Model.DateAndTime</small>
}