@using WeatherApp.Models;

@model CityModel;

@if(Model != null)
{
    <h3>@Model.CityUniqueCode</h3>
    <p>@Model.CityName - <span><a href="" style="text-decoration:none;"> View Details</a></span></p>
    <p>@Model.TemperatureFahrenheit</p>
    <small>@Model.DateAndTime</small>
    <hr />
}